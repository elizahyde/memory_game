// Click a tile 1. Activate tile unless already active. Count click.

// If selection length == 2, AND no match, activate tile unless already active. Count click.

// If selection length == 2, AND match, fade out, no longer selectable. Count click.